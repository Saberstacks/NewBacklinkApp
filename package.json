{
  "name": "backlink-tracking",
  "version": "1.0.0",
  "description": "Backlink Tracking Feature using DataForSEO API",
  "scripts": {
    "build": "echo 'No build step required'",
    "start": "vercel dev"
  },
  "dependencies": {
    "axios": "^1.4.0"
  },
  "engines": {
    "node": ">=18"
  }
}
